'use client';

import Loading from '@/components/ui/Loading';

export default function WarrantyCheckLoading() {
    return (
        <Loading 
            title="Đang tải kiểm tra bảo hành"
            message="Đang tải trang kiểm tra bảo hành..."
        />
    );
}