'use client';

import Loading from '@/components/ui/Loading';

export default function ContactLoading() {
    return (
        <Loading 
            title="Đang tải liên hệ"
            message="Đang tải thông tin liên hệ..."
        />
    );
}