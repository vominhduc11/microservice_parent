'use client';

import Loading from '@/components/ui/Loading';

export default function CertificationLoading() {
    return (
        <Loading 
            title="Đang tải chứng nhận"
            message="Đang tải thông tin chứng nhận..."
        />
    );
}