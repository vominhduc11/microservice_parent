'use client';

import Loading from '@/components/ui/Loading';

export default function AboutLoading() {
    return (
        <Loading 
            title="Đang tải giới thiệu"
            message="Đang tải thông tin về công ty..."
        />
    );
}